# Camunda LangGraph Workflow

Angular BPMN modeler for designing Camunda-compatible XML and converting it into LangGraph workflow code in TypeScript, JavaScript, Python, and JSON.

## Run

```bash
npm install
npm start
```

Open `http://localhost:4200/`.
